# About

This Python script was written to replace PuTTY, which I found difficult to use. This script serves a single purpose: capturing data from a COM port. If the connection is lost, the script remains active instead of crashing.

# Requirements

The only dependency required is `pyserial`.

```SHELL
pip install pyserial
```
